version: '3.8'

services:
# Servicio principal - Pipeline ML
  home-credit-ml:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: home_credit_ml_pipeline
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - models_volume:/app/data/06_models
    environment:
      - KEDRO_ENV=local
      - PYTHONPATH=/app/src
    networks:
      - home_credit_network
    restart: unless-stopped
    command: ["kedro", "run"]

  # Kedro Viz - Visualización del pipeline
  kedro-viz:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: home_credit_ml_viz
    ports:
      - "4141:4141"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./conf:/app/conf
      - ./src:/app/src
    environment:
      - KEDRO_ENV=local
      - PYTHONPATH=/app/src
    networks:
      - home_credit_network
    restart: unless-stopped
    command: ["kedro", "viz", "--host=0.0.0.0", "--port=4141", "--no-browser"]
    depends_on:
      - home-credit-ml

  # Jupyter Lab - Desarrollo y análisis
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: home_credit_ml_jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./:/app
      - jupyter_data:/home/credit_user/.jupyter
    environment:
      - KEDRO_ENV=local
      - PYTHONPATH=/app/src
      - JUPYTER_ENABLE_LAB=yes
    networks:
      - home_credit_network
    restart: unless-stopped
    command: ["bash", "-c", "pip install jupyter kedro[jupyter] && jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root --NotebookApp.token='' --NotebookApp.password=''"]

volumes:
  models_volume:
    driver: local
  jupyter_data:
    driver: local
  dev_cache:
    driver: local

networks:
  home_credit_network:
    driver: bridge